# 一些java基础技术




#### java基础:

**数据结构:**

1、指向不同：单向链表只有一个指向下一结点的指针，双向链表除了有一个指向下一结点的指针外，还有一个指向前一结点的指针。
2、功能不同：单向链表只能next ，双向链表可以return。
3、单双向不同：单链表只能单向读取，双向链表可以通过prev()快速找到前一结点。

**sql**:

行转列就是case when, group_cat eg: 3列性命, 科目, 分数 变成 姓名行 科目列 分数在中间
列转行 union all

over(PARTITION by)的使用

#### 算法:

- 循环不变式：初始化，保持，终止
- 使用RAM模型，不考虑虚拟内存，高速缓存，多核处理，简化模型后仍然面临很多选择和困难
- Merge_sort中，书中给的例子是将，一个数组，分成两部分存在两个数组里，由两个数组的头部元素进行比较，在存储到原数组里。
- 设计算法，首先得定义问题，成本模型或者? 模型， 设计API， 用例代码

1. 选择排序(selection sort)：a[]分为指针i=0,i to a.size() i++, 永远查找最小的元素和a[i]交换
2. 插入排序(insert sort)：一个数组，使其前半部分永远处于排序状态，将后部分元素，分别插入前部分，
   a1, a2, a1所有的元素向右移1,a2-1，直到a2为0； 注意：是找到位置在插入，不是一直交换
3. 希尔排序(Shell sort)：
4. 快速排序(Quric sort)： 以中间为基点，小的放左边，大的放右边，然后递归。
5. 合并排序：
6. 优先队列：

#### 设计模式:

- 观察者模式应对一对多，一个观察者改变，所有依赖它的都要改变，设计observe，subject接口分别对应，观察模式的对象方法。进行实现
- 还是不太清楚装饰者模式的运行原理，它用子类扩展了父类。用需要装饰的对象继承子类或父类，以此进行不需要修改的运行？？例子是：java I/O
- 关于咖啡这有三种实现：
  1第一设一个beverage（饮料）的父类，所有子类继承，这样
  添加，删除都会很麻烦
  2设变量和方法的父类，继承

适配器模式, 将两个new类结合到一起, 继承一个, 在内部创建一个

#### 网络:

osi模型
计算机网络 1.4 51
物理: 光纤, wifi, 何为1何为0
数据链路: 原始的传输设施变成逻辑的传输线路.把数据拆开, 封装到数据帧
网络层: 将分组从源端路由目标端
传输层: 接受上层数据, 给下层使用. 是一个真正端到端的层.
会话层:
表示层:
应用层:
